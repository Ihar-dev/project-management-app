import { GetUserNameDirective } from './get-user-name.directive';

describe('GetUserNameDirective', () => {
  it('should create an instance', () => {
    const directive = new GetUserNameDirective();
    expect(directive).toBeTruthy();
  });
});
