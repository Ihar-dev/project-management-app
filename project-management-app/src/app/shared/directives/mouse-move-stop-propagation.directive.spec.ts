import { MouseMoveStopPropagationDirective } from './mouse-move-stop-propagation.directive';

describe('MouseMoveStopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseMoveStopPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
