import { MouseClickStopPropagationDirective } from './mouse-click-stop-propagation.directive';

describe('MouseClickStopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseClickStopPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
