/* import { RandomPhotoBackDirective } from './random-photo-back.directive';

describe('RandomPhotoBackDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomPhotoBackDirective();
    expect(directive).toBeTruthy();
  });
}); */
