export enum ErrorMessage {
  forbidden = 403,
  success = 200,
}

export const USER_TOKEN_KEY = '___user-token___';

export enum Url {
  SIGN_UP = 'signup',
  SIGN_IN = 'signin',
  USERS = 'users',
}
